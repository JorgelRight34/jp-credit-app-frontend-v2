import { PROJECT_ID_STORAGE_KEY } from "@/features/projects/lib/constants";
import { ACCESS_TOKEN_KEY } from "./local-storage-constants";

export const COOKIES = {
    ACCESS_TOKEN: ACCESS_TOKEN_KEY,
    PROJECT_ID: PROJECT_ID_STORAGE_KEY
}