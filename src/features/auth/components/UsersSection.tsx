/*
import UsersDataTable from "./UsersDataTable";
import { User } from "../models/user";
import UserQuerySearch from "./UserQuerySearch";
import { UserQuery } from "../models/userQuery";
import { EntitySectionProps } from "@/components/EntitySection";
import EntitySection from "@/components/EntitySection/components/EntitySection";

type UsersSectionProps = EntitySectionProps<User, UserQuery>;

const UsersSection = ({ ...props }: UsersSectionProps) => {
  return (
    <EntitySection
      Search={UserQuerySearch}
      DataTable={UsersDataTable}
      {...props}
    />
  );
};

export default UsersSection;
*/
const Default = () => <></>

export default Default
