import { FormLabelProps, FormLabel as MFormLabel } from '@mui/material'

const FormLabel = (props: FormLabelProps) => (
  <MFormLabel {...props} className="text-secondary w-full" />
)

export default FormLabel
