export const SX_CONFIG = {
    borderRadius: "0.5rem", // Tailwind's rounded-xl = 1rem
    "& .MuiOutlinedInput-root": {
        borderRadius: "0.5rem", // make the outer input wrapper rounded,
    },
}
